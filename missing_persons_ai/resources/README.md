# resources/

This folder stores uploaded images for registered missing persons and public submissions.

- Files are auto-saved here when cases are registered or sightings are submitted.
- Images are named by their UUID (e.g. `a3f9c2d1-....jpg`).
- This folder is excluded from git via `.gitignore`.
